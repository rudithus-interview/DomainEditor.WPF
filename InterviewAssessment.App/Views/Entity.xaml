<UserControl x:Class="InterviewAssessment.App.Views.Entity"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Height="50" Width="80">
    <Grid>
        <Rectangle RadiusX="4" RadiusY="4" Stroke="Black" Fill="LightBlue"/>
        <Label Content="{Binding Path=Text}"/>
        <Grid.ContextMenu>
            <ContextMenu>
                <Button Click="MenuItem_Click">Attributes</Button>
            </ContextMenu>
        </Grid.ContextMenu>
    </Grid>
</UserControl>
